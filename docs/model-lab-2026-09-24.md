# Model lab — 2026-09-24

The protocol for every experiment: tune on Tier-1 matches from 2023–24, then score 2025 and 2026 without retuning, using a paired per-match log-loss t-test against production Elo. The ship bar in CLAUDE.md is a consistent gain with |t| > 2.
2025 and 2026 have both been used for model selection before, so passing here is a precondition for shipping, not proof.

## 0. Data fix: split team identities (applied)

The event feed carries team names only. That left 61 Tier-1 teams with no vlr.gg team ID, so each started over as a new 1500-rated team. They were in 251 matches since 2023, including G2 (193 matches), FPX, NRG, ENVY, KIWOOM DRX and JD Gaming.
I fetched match details for the latest match of each of those teams, and `vct load-vlrgg-details` then resolved 1,583 rows. Since 2023, 0 Tier-1 matches still have an unresolved team.

| Production Elo | 2023-24 | 2025 | 2026 |
|---|---:|---:|---:|
| before fix | 0.6326 | 0.6485 | 0.6736 |
| after fix  | 0.6326 | 0.6485 | **0.6674** (t = +1.17) |

Walk-forward pooled: 0.6589 → **0.6567**.
Live effect: NRG had been rated as an 8-match newcomer, so Nongshim vs NRG moved from 75% to 53% (the market price is 50.5%).

## 1. Candidates

`scripts/model_lab.py`, `model_lab2.py`, `ensemble_robustness.py`

| Candidate | Tuned setting | 2025 | 2026 | Held-out 2025+26 t | Verdict |
|---|---|---:|---:|---:|---|
| Production Elo | K=48, map share | 0.6485 | 0.6674 | — | baseline |
| Best-of-aware map Elo | K=40, scale 800 | 0.6665 | 0.6895 | −4.24 | **rejected** |
| Inactivity decay (calendar days) | half-life 730d (edge) | 0.6424 | 0.6726 | +0.00 | rejected |
| Player-level Elo blend | w=0.25, K_p=24 | 0.6450 | 0.6696 | +0.31 | rejected |
| Online shrink calibration | trailing 500 | 0.6458 | 0.6642 | +1.21 | not proven |
| Stacked logistic (Elo + form + churn + exp) | walk-forward yearly refit | — | — | — | incomplete: the run timed out after 6 of 27 settings (the Elo-only refits), which scored 0.654–0.680 on validation vs 0.633 |
| **Fast/slow Elo ensemble** | 0.7·K16 + 0.3·K256 (logit blend) | **0.6381** | **0.6637** | **+1.70** | **promising, not shipped** |

## 2. The fast/slow ensemble

This is a logit-space blend of two Elo ratings: a slow one (K≈16) that tracks organisation strength, and a fast one (K≈128–256) that reacts to current form.

- **Wins every walk-forward fold** (benchmark_elo protocol): pooled **0.6488 vs 0.6567** log loss, Brier 0.2285 vs 0.2320, accuracy 62.6% vs 61.6%.
- **Broad plateau, not a spike:** 82% of the 330 nearby settings improve on held-out data, with median t = +1.60.
- **But no single pre-selected setting clears t > 2 in both years.** The round-2 pick (0.6·K16 + 0.4·K128) reached t = +2.99 held-out. The wider grid's validation pick reached only t = +1.70, and 2026 alone is t = +0.69.
- Calibration is still overconfident at 60–80%, the same pattern as raw Elo.

**Recommendation:** run the ensemble in shadow. Log its probability next to Elo in `prediction_log.parquet` and let `grade_predictions.py` score it on live matches. Promote it only if it keeps its edge there.

## 3. Rejected ideas: what we learned

- **Best-of-aware ratings hurt badly.** Treating ratings as per-map strength and compounding them to a series probability makes favourites far too confident, and a wider scale did not fix it.
- **Inactivity decay and player-level Elo** add nothing beyond what K=48 already learns.
- **Refitting a logistic layer on Elo alone lost in every setting that finished.** The versions with player form, churn and experience were never scored. The mapping from rating to probability drifts across eras (see the calibration notes in CLAUDE.md), so fitting on past years hurts.
